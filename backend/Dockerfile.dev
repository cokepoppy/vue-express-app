FROM node:18-alpine

WORKDIR /app

# 安装 nodemon 全局
RUN npm install -g nodemon

# 复制 package 文件
COPY package*.json ./

# 安装依赖
RUN npm install

# 复制源代码
COPY . .

# 暴露端口
EXPOSE 5000

# 开发模式启动
CMD ["npm", "run", "dev"]