# Vercel 部署环境变量模板
# 请将这些环境变量配置到 Vercel 项目设置中

# =================================
# Supabase 配置 (必须配置)
# =================================
# 获取方式：登录 Supabase -> Project Settings -> Database
DATABASE_URL=postgresql://postgres:[YOUR-PASSWORD]@db.[PROJECT-REF].supabase.co:5432/postgres

# 获取方式：登录 Supabase -> Project Settings -> API
SUPABASE_URL=https://[PROJECT-REF].supabase.co
SUPABASE_ANON_KEY=[YOUR-ANON-KEY]

# =================================
# Upstash Redis 配置 (可选，推荐)
# =================================
# 获取方式：登录 Upstash -> Redis Database -> Details
UPSTASH_REDIS_REST_URL=https://[REDIS-ID].upstash.io
UPSTASH_REDIS_REST_TOKEN=[REDIS-TOKEN]

# =================================
# 应用配置 (自动配置)
# =================================
NODE_ENV=production
PORT=5000

# =================================
# 备用配置 (自动回退)
# =================================
# 如果没有 Upstash Redis，可以使用其他 Redis 服务
REDIS_URL=redis://localhost:6379

# MongoDB 备选 (如果不用 PostgreSQL)
MONGODB_URI=mongodb://localhost:27017/vue-express-app

# JWT 配置 (可选)
JWT_SECRET=your-jwt-secret-key
JWT_EXPIRES_IN=7d

# =================================
# 配置说明
# =================================

# 1. 在 Vercel 项目设置中添加以上环境变量
# 2. 至少需要配置 DATABASE_URL, SUPABASE_URL, SUPABASE_ANON_KEY
# 3. Redis 配置可选，不配置将跳过缓存功能
# 4. 配置完成后需要重新部署项目

# =================================
# 部署命令
# =================================
# npm run deploy:vercel
# 或者
# ./scripts/deploy.sh